[project]
name = "qr-mac"
version = "0.1.0"
description = "Small macOS desktop QR scanner with Wi-Fi QR support"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python==4.11.0.86",
    "PySide6==6.9.0",
]

[tool.uv]
package = false